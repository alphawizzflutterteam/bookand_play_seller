import 'package:flutter/material.dart';

class ground_listing extends StatefulWidget {
  const ground_listing({Key? key}) : super(key: key);

  @override
  State<ground_listing> createState() => _ground_listingState();
}

class _ground_listingState extends State<ground_listing> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
